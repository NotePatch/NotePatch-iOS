# 2026-07-12

## NotePatch UI 全面重设计（按设计 Brief）

用户提供了一份完整的设计 Brief，要求将全部 UI 从之前的液态玻璃风格改成：
- 暖中性调色板（#F8F9F7 背景，#FFFFFF 卡片，#171717 主文字，#737373 副文字）
- 品牌绿色点缀（#7EB88B，仅 8% 使用率）
- 8pt 网格间距系统
- 极简 Apple HIG 风格——无玻璃、无渐变、无彩色背景
- 白色卡片 + 1px 边框 + 极淡阴影（opacity 8%）

### 改动

**新建 `DesignSystem.swift`**（253 行）：
- `NPColors`：全套颜色 token（背景/卡片/文字/边框/品牌绿/语义色）
- `NPSpacing`：8pt 网格间距 token
- `NPRadius`：统一圆角系统（18/14/12/20）
- `NPCardModifier`：白色卡片 + 1px border + 极淡阴影
- `NPPrimaryButtonStyle` / `NPSecondaryButtonStyle` / `NPIconButtonStyle`
- `NPStatusChip`：统一状态药丸组件
- `NPEmptyState`：统一空状态组件
- `NPSection`：卡片包装器
- 排版扩展：`.npScreenTitle()` / `.npSectionTitle()` / `.npCardTitle()` / `.npBody()` / `.npCaption()`
- 动画预设：`.npInteractive` / `.npCardEntry` / `.npSheetSpring`
- `Color(hex:)` 扩展

**重写 `ContentView.swift`**（3387 行）：
- 全部 40 个 view struct 从玻璃风格转换为极简暖白风格
- 0 个 glassEffect / liquidGlass / .glass 引用残留
- 所有颜色/间距/排版/圆角统一用设计 token
- 所有按钮用 `NPPrimaryButtonStyle` / `NPSecondaryButtonStyle`
- 所有状态指示器用 `NPStatusChip`
- 所有空状态用 `NPEmptyState`

**删除 `LiquidGlass.swift`**——已无引用。

### 编译状态
xcodebuild **BUILD SUCCEEDED**。

---

## 全英文翻译 + Olivia Rodrigo 手写字体标题（22:20 - 22:30）

### 翻译覆盖
- `NotePatchViewModel.swift`：90+ 处中文字符串 → 英文（tab 名、状态消息、错误提示、离线测试数据）
- `AppSupport.swift`：40+ 处（statusLabel、documentKindLabel、fileTypeLabel、friendlyError）
- `Models.swift`：4 处（revisionOriginLabel）
- `TusUploader.swift`：9 处（上传错误消息）
- `LearningBackendClient.swift`：11 处（HTTP 错误消息）
- `ContentView.swift`：100+ 处（所有 UI 标签、按钮、提示、placeholder）

### Olivia 字体系统（DesignSystem.swift）
- `Font.oliviaTitle`：26pt custom "OliviaRodrigoHand"（fallback → system serif bold）
- `View.oliviaTitle()` modifier：font + 8pt 垂直 padding（保护手写 swirl loop 不切边）
- 应用到 "Notes"、"Documents"、"Review" 三个主屏幕标题
- iOS 15 兼容：跳过 `.tracking()`（iOS 16+ only），用 padding 替代

---

## UI 精简优化（22:09 - 22:20）

用户要求对特定组件做精细瘦身：

### DesignSystem.swift 新增
- `NPDocumentPrimaryButtonStyle`：白底 + 品牌绿 40% 描边 + 品牌绿文字，36pt 高度
- `NPDocumentIconButtonStyle`：无边框/无背景/深灰裸图标，no frame
- `NPToolbarIconButtonStyle`：纯图标 + 淡绿圆形背景(20% opacity)，36x36
- `NPStatusChip` `.brand` bg：从 full `NPColors.brandLight` → `opacity(0.30)`
- `NPInputFieldModifier`：白底 + 14px 圆角 + 1px `#E8E8E8` 边框，`.npInputField()` 扩展

### ContentView.swift 修改
- **Tab Bar**：通过 `UITabBarAppearance` 强制透明背景，去掉选中态背景胶囊，选中态用 `NPColors.brandDark`，未选中用 65% 灰色
- **LabeledField**：`.background(NPColors.divider)` → `.npInputField()`（白底+细边框）
- **TextEditor**（作业评分）：`.background(NPColors.divider.opacity(0.6))` → `.npInputField()`
- **作业评分 Header**：`+` 按钮 `.buttonStyle(NPPrimaryButtonStyle())` → `NPToolbarIconButtonStyle()`；`↻` 同样改
- **文档卡片按钮**：重处理 `.NPPrimaryButtonStyle`(48pt绿块) → `.NPDocumentPrimaryButtonStyle`(36pt白底+绿描边)；下载和更多 `.NPSecondaryButtonStyle`(带框) → `.NPDocumentIconButtonStyle`(裸图标)
