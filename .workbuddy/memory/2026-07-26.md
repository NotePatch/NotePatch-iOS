# 2026-07-26 Work Log

## Design System v3.0 Specification
- Created complete Design System doc at `docs/DesignSystem.md`
- Covers: Color (background hierarchy, brand Sage, semantic, text, dividers), Shadow (small/medium/large/hover), Radius (small/medium/large/xl), Spacing (9 tokens on 4pt grid: 4→48), Typography (6 semantic roles with Dynamic Type), Animation (6 tokens from instant to breath), Elevation (5 Z-levels base→overlay)
- Key changes from v2: removed semantic spacing aliases (card=18, section=24 etc.) in favor of raw scale; renamed shadow tokens to semantic names; typography now uses Dynamic Type styles instead of fixed pt sizes; 4 new spacing tokens added (20, 32, 40, 48); component-to-token mapping table added; design audit checklist added; migration path documented
- Visualizations: color palette, spacing scale, elevation model
- Role established: Lead iOS Product Designer + Senior SwiftUI Engineer for NotePatch
- Identity files updated at ~/.workbuddy/ (SOUL.md, IDENTITY.md, USER.md), BOOTSTRAP.md deleted

## Design System v3.0 Implementation
- Updated `DesignSystem.swift` to v3.0 spec:
  - Spacing: added xxs(4), large(20), xxl(32), xxxl(40), huge(48); removed ambiguous aliases; legacy aliases kept as deprecated
  - Shadow: renamed level1→medium, level2→small, level3→hover; added large(6%/40/16) for sheets/modals
  - Typography: replaced fixed pt sizes with Dynamic Type styles (title2/headline/body/caption etc.); renamed npScreenTitle→npTitle, npSectionTitle→npHeading, npCardTitle→npSubheading; added npCallout; legacy APIs kept as deprecated
  - Animation: added npInstant, npQuick, npBreath; renamed npInteractive for consistency
  - Elevation: added NPElevation struct (base/raised/card/sheet/overlay)
  - Color: added brandGlow token (#C8EFCF) for hover effects
- ContentView.swift hardened: 80+ replacements across 5 categories
  - 4x .foregroundStyle(.white)/.tint(.white) → NPColors.surface
  - 3x hardcoded shadows → NPShadow.small
  - 4x hardcoded cornerRadius → NPRadius.xs/small
  - 42x hardcoded padding/spacing → NPSpacing tokens (matching exact values only)
  - 15x .font(.caption/.body/.subheadline/.headline/.title3) → npXxx() modifiers
  - 1x .foregroundColor → .foregroundStyle (modern API)
  - 2x cornerRadius(2/6) retained as legitimate edge cases (code accent bar, thumbnail)
  - 1x Color.black in ImagePreview retained (full-screen preview, HIG convention)
- All 11 other Swift files verified: zero hardcoded design values

## Apple Design Review — 10 Issues Fixed
Full audit of ContentView.swift (3900+ lines) identified and fixed:

1. **Shadow elevation** — StatusBanner + StatusPanel now have `NPShadow.small` for depth separation from content
2. **Deprecated typography** — 18x instances of npScreenTitle/npSectionTitle/npCardTitle → npTitle/npHeading/npSubheading
3. **Profile tab brand header** — logo padding (large→item, small→xs) and tagline (14pt/0.85→13pt/0.7) now match other 3 tabs
4. **Notification radius** — StatusPanel unified to `NPRadius.medium` (14) from card (20); StatusBanner already at button(14)=medium
5. **ChoiceButton grid stretching** — replaced NPPrimaryButtonStyle/NPSecondaryButtonStyle with local `ChoiceChipButtonStyle` (no maxWidth:.infinity, proper chip sizing)
6. **Icon squareness** — Flashcard nav buttons 42×36 → 36×36 (square, matches toolbar icons)
7. **Divider color misuse** — editor background: divider.opacity(0.35)→NPColors.background; menu button: divider→interactive.opacity(0.4)
8. **Composer dual elevation** — removed border stroke from expanded state, kept NPCardShadow only
9. **StatusBanner bg opacity** — success state: brandLight.opacity(0.6→0.5) to match StatusPanel
10. **Subheadline font** — 12x `.font(.subheadline.weight(...))` → `.npBody().fontWeight(.medium)` (9x medium) + `.npSubheading()` (3x semibold)
- `.padding(.top, 14)` pattern (3 occurrences) documented as future token gap; kept as-is to preserve spacing
- SectionLabel redundant `.foregroundStyle(NPColors.textPrimary)` removed (npSubheading already sets it)
- Remaining 32x `.font(.system(...))` calls are all legitimate edge cases (ButtonStyle internals, decorative icons, monospaced code, markdown rendering)

## iOS 18 Modernization
- Deployment target: iOS 15.0 → 18.0 (6 build configs in project.pbxproj)
- 4x NavigationView → NavigationStack (iOS 16+)
- 3x .navigationViewStyle(StackNavigationViewStyle()) → removed (obsolete with NavigationStack)
- 2x .sheet(onDismiss:) → .onDisappear (iOS 17+ pattern)
- 1x UIApplication.shared.sendAction → connectedScenes.endEditing (scene-based API)
- 1x TabBar: added .toolbarBackground modifiers alongside retained UITabBarAppearance config
- 1x .ultraThinMaterial → .thinMaterial (iOS 18+ modern Material)
- Build result: **BUILD SUCCEEDED** — 0 errors, 0 warnings
- Migration report at docs/Migration-Report.md
- Intentional deferrals: @Observable migration (touches 6 classes, 24 properties — needs dedicated session); all other APIs already modern
