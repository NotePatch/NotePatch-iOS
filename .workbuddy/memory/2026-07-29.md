# 2026-07-29 Work Log

## Visual Hierarchy Refinement — Three-Section Page Structure
Brand → Page Header → Controls → Content. Applied across all tabs.

### Brand Section (all 4 tabs)
- Logo height: 64→90pt (+40%), AuthScreen: 56→78pt
- Logo top padding: `NPSpacing.item`(16)→`NPSpacing.large`(20)
- Logo bottom padding: `NPSpacing.xs`(4)→`NPSpacing.small`(8)
- Tagline bottom: `NPSpacing.item`(16)→`NPSpacing.xl`(24)
- Tagline color: `textSecondary.opacity(0.7)`→`NPColors.textTertiary` (clean semantic token)
- Profile tab: alignment center→leading, logo 100→90

### Documents Tab — 3-section restructure
1. **Brand** — logo + tagline (unchanged structure)
2. **Page Header** — extracted "Documents" title (npSubheading) + count chip from ScrollView to main VStack, 20pt below brand
3. **Controls** — segmented control + filter button (inline with it), 16pt below header
4. **Content** — ScrollView with filter panel + document cards, `.top NPSpacing.small`

### Notes Tab
- Removed unnecessary 48pt frame around refresh button
- Refresh button moved to same row as segmented control (HStack)
- Controls section: `.bottom NPSpacing.item`

### Profile Tab
- Logo: 100→90pt, `alignment: .center`→`.leading`, tagline `.center`→`.leading`

### NavigationView Cleanup
- 4x NavigationView → NavigationStack (StudyNoteEditor sheet, UploadScreen inner, HomeworkCreateSheet, NotesTab readerSheet)
- 0x navigationViewStyle remaining
- Build: 0 errors, 0 warnings

## Home Tab Rename
- Tab icon: doc.text → "house" (NotePatchViewModel.swift)
- Page header: "Documents" → "Home" (ContentView.swift)
- All 3 localizations: "Documents"→"Home", "文档"→"主页", "文件"→"主頁"

## Navigation Layer Redesign — Upload Button Architecture
- Moved Upload button from ZStack overlay into `.safeAreaInset(edge: .bottom)` alongside FloatingTabBar
  - Layer 2: UploadActionButton (fixed, 10px above nav bar, right-aligned, 20px edge margin)
  - Layer 1: FloatingTabBar (4 tabs)
  - Content area properly inset — scrollable content never hidden behind button
- UploadActionButton restyled as Liquid Glass capsule:
  - Height: 52px, Capsule shape (999px radius)
  - Background: `.regularMaterial` (≈60-70% opacity, 28-36px blur equivalent)
  - Border: `.white.opacity(0.20)`, 1px — translucent edge highlight
  - Brand green "+" only, label uses adaptive text color (primary in light/dark)
  - Shadow: `.black.opacity(0.03), radius 8, y 2` — very soft
  - Press: 0.96 scale with `.spring(response: 0.28, dampingFraction: 0.78)`
- Removed old GlobalUploadButton struct (replaced by UploadActionButton)
- Removed unused ZStack(alignment: .bottomTrailing) wrapper
- Build: 0 errors, 0 warnings
